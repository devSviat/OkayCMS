-- Оцінка в зірках усередині відгуку.
--
-- Виконувати РАЗОМ із розгортанням коду: без колонки CommentsEntity оголошує
-- поле, якого немає в таблиці, і будь-яка вибірка коментарів впаде.
--
-- Середній бал товару після цього рахується з відгуків (RatingHelper::
-- recalculateFromReviews), а не окремим анонімним голосуванням. Поля рейтингу
-- в адмінці товару та поста прибрані — значення похідне.

ALTER TABLE `ok_comments` ADD `rating` TINYINT NULL DEFAULT NULL AFTER `user_id`;
