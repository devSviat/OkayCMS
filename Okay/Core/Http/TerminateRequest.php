<?php

namespace Okay\Core\Http;

/**
 * Керований вихід із запиту замість exit: код нижче не виконується, але процес
 * лишається живим. Під php-fpm результат той самий, що й від exit, у worker
 * mode - різниця між теплим процесом і перезапуском скрипта на кожному редіректі.
 *
 * Розширює Error, а не Exception, свідомо: ядро й модулі ловлять
 * `catch (\Exception)` навколо контролерів, і виняток-нащадок Exception
 * перетворив би редірект на порожню 200.
 */
final class TerminateRequest extends \Error
{
}
