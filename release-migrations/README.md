# release-migrations/

Core-специфічні DB-міграції форку — вхід збірки пакета релізу
(`ok release:build-package`). Порожньо, поки жодна версія не міняла схему.

Формат імен, вимога унікальності, правило «файли звідси не видаляють» і
обмеження діалекту SQL —
[docs/updates.md § Core-міграції форку](../docs/updates.md#core-міграції-форку).
