# У корені контексту, а не поруч із Dockerfile: per-Dockerfile ignore-файл
# читає лише BuildKit, класичний білдер його мовчки ігнорує.

# Гітігнорений, але контекст читає файлову систему, а не git — без цього рядка
# пароль дев-бази запікся б в образ.
config/config.local.php

# Механізму Docker secrets тут уже немає, ігнор лишається на випадок повернення.
dev/secrets

dev/docker-compose.local.yml

.git
vendor
node_modules

dev/.env
dev/mysql
dev/logs
dev/bin

compiled/*/*.php
backend/design/compiled/*.php
cache
Okay/log
.phpunit.result.cache
